[build]
buildCommand = "npm install && cd client && npm install && npm run build && cd ../api && npm install && npx prisma generate"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3