[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build && cd api && npx prisma generate && npx prisma migrate deploy"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "api"

[[services]]
name = "postgres"
